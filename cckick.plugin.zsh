# cckick.plugin.zsh — thin entry for plugin managers (oh-my-zsh / zplug / zinit / antigen)
# to load with zero extra config. The real logic lives in cckick.zsh.
# ${0:A:h} = absolute directory of this file.
source "${0:A:h}/cckick.zsh"
